/**
 * @description this enum define some specific options we excpect to get in the param body request(genre)
 * *thath means the option for ganre we allow to get in the body are those we define here*
 */
export enum genre {
  HORROR = 'horror',
  ROMANTIC = 'romantic',
  SCIENCE_FICTION = 'science_fiction',
  COMEDY = 'comedy',
  ACTOIN = 'action',
}
